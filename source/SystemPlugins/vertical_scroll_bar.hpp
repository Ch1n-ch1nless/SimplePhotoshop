#ifndef  SYSTEM_PLUGINS_VERTICAL_SCROLL_BAR_HPP
#define  SYSTEM_PLUGINS_VERTICAL_SCROLL_BAR_HPP

#include "../View/View.hpp"

namespace sys_plugin
{
    /*class Slider : public ps::AWindow
    {
    public:


    private:

    };

    class VerticalScrollBar : public ps::AWindowVector
    {
    public:
         VerticalScrollBar();
        ~VerticalScrollBar() = default;

        virtual void draw(psapi::IRenderWindow* renderWindow)         override;
        virtual bool update(const psapi::IRenderWindow* renderWindow,
                            const psapi::sfm::Event& event)           override;

        virtual psapi::wid_t    getId()                         const   override;
        virtual IWindow*        getWindowById(psapi::wid_t id)          override;
        virtual const IWindow*  getWindowById(psapi::wid_t id)  const   override;
        virtual ps::vec2i       getPos()                        const   override;
        virtual ps::vec2u       getSize()                       const   override;
        virtual void            setParent(const IWindow* parent)        override;
        virtual void            forceActivate()                         override;
        virtual void            forceDeactivate()                       override;
        virtual bool            isActive()                      const   override;
        virtual bool            isWindowContainer()             const   override;

        static std::unique_ptr<VerticalScrollBar> create();
    private:
        ps::Transformable*  object;
    };*/
}

#endif //SYSTEM_PLUGINS_VERTICAL_SCROLL_BAR_HPP